# apps/web env (non-Docker local dev). Copy to .env and fill in.
# VITE_ vars are inlined into the client bundle at build time.

# Origin of the server the browser calls for /rpc and /api/auth.
VITE_SERVER_URL=http://localhost:3000
